% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generator.R
\name{make_marksheet}
\alias{make_marksheet}
\title{config からマークシート(.tex)と読み取り定義を生成する}
\usage{
make_marksheet(
  config = default_config(),
  tex_path = NULL,
  marks_path = NULL,
  fiducials_path = NULL
)
}
\arguments{
\item{config}{\code{default_config()} と同形の list。}

\item{tex_path}{書き出す \code{.tex} のパス（NULL なら書き出さない）。}

\item{marks_path}{書き出す marks CSV のパス（NULL なら書き出さない）。}

\item{fiducials_path}{書き出す fiducials CSV のパス（NULL なら書き出さない）。}
}
\value{
\code{list(tex=, marks=, fiducials=, id_prefix=)}。marks/fiducials/id_prefix は
そのまま \code{read_marksheet()} の \code{layout} に渡せる（接頭辞が自動で引き継がれる）。
}
\description{
同じ config から，組版用 \code{.tex} と，読み取り用の \code{marks}（field,value,x_mm,y_mm）・
\code{fiducials}（corner,x_mm,y_mm）を生成する。両者は座標が一致する。
}

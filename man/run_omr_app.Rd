% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app.R
\name{run_omr_app}
\alias{run_omr_app}
\title{マークシート工房（ローカル Shiny GUI）を起動する}
\usage{
run_omr_app(...)
}
\arguments{
\item{...}{\code{shiny::runApp()} に渡す引数（\code{port}, \code{launch.browser}, \code{host} など）。}
}
\value{
起動した Shiny アプリ（\code{shiny::runApp()} の戻り値）。副作用としてアプリを起動する。
}
\description{
マークシートの生成（config → \code{.tex} / PDF / 読み取り定義）と読み取り
（スキャン → responses.csv / review.csv）をブラウザ上の GUI で行う。
すべてこの端末上で動き、答案画像は外部に送信されない。
}
\details{
生成タブで PDF を書き出すには \code{lualatex}（LuaLaTeX）がパスに必要。無い場合は
\code{.tex} と読み取り定義 CSV のみ書き出せる（PDF ボタンは無効表示）。
}
\examples{
\dontrun{
run_omr_app()
}
}

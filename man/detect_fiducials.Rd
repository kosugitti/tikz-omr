% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fiducials.R
\name{detect_fiducials}
\alias{detect_fiducials}
\title{四隅の位置決めマークを検出する}
\usage{
detect_fiducials(gm, dark = 140, frac = 0.22, rowthr = 25)
}
\arguments{
\item{gm}{グレースケール行列 \code{gm[x, y]}（0-255，x=列/幅，y=行/高）。}

\item{dark}{暗画素とみなす閾値（既定 140）。}

\item{frac}{各隅で探索する象限の割合（既定 0.22）。}

\item{rowthr}{暗ブロックとみなす暗画素数の下限（既定 25）。}
}
\value{
\code{list(TL=, TR=, BL=, BR=)}。各要素は \code{list(cx, cy, side)}。
}
\description{
マークシート四隅の黒い四角（左上のみ大きい）を，隅近傍の暗画素射影から矩形として
切り出す。OpenCV 相当の輪郭検出を使わず \code{magick} のグレースケール行列のみで動く。
}
